% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/guide.R
\name{guide_tintshade}
\alias{guide_tintshade}
\title{Legend guide for the tintshade aesthetic}
\usage{
guide_tintshade(
  title = ggplot2::waiver(),
  type = c("auto", "unique", "crossed"),
  ...
)
}
\arguments{
\item{title}{Legend title. Defaults to the mapped variable name.}

\item{type}{How to colour the legend keys:
\itemize{
\item \code{"auto"} (default): a key is drawn in its hue when its tintshade level
maps to a single hue (a nested design) and in neutral grey when it
co-occurs with several (a crossed design, where lightness is
hue-independent).
\item \code{"unique"}: always use the hue.
\item \code{"crossed"}: always use neutral grey.
}}

\item{...}{Passed to \code{\link[ggplot2:guide_legend]{ggplot2::guide_legend()}}.}
}
\value{
A guide object.
}
\description{
A \code{\link[ggplot2:guide_legend]{ggplot2::guide_legend()}} whose keys are recoloured to match the tinted
layer. The tintshade scale uses this guide by default.
}
\examples{
library(ggplot2)
df <- expand.grid(hue = c("good", "bad"), level = c("lo", "hi"))
df$y <- 1
ggplot(df, aes(hue, y, fill = hue, tintshade = level)) +
  geom_col_tintshade(position = "dodge") +
  guides(tintshade = guide_tintshade(type = "crossed"))
}
\seealso{
\code{\link[=scale_tintshade_discrete]{scale_tintshade_discrete()}}
}
