% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale.R
\name{scale_tintshade}
\alias{scale_tintshade}
\alias{scale_tintshade_discrete}
\alias{scale_tintshade_continuous}
\title{Lightness (tintshade) scales}
\usage{
scale_tintshade_discrete(..., range = c(0.2, 0.8), aesthetics = "tintshade")

scale_tintshade_continuous(..., range = c(0.2, 0.8), aesthetics = "tintshade")
}
\arguments{
\item{...}{Passed to \code{\link[ggplot2:discrete_scale]{ggplot2::discrete_scale()}} or
\code{\link[ggplot2:continuous_scale]{ggplot2::continuous_scale()}}.}

\item{range}{Numeric length-2 output range within \verb{[0, 1]}. Defaults to
\code{c(0.2, 0.8)} (dark to pale, base hue in the middle).}

\item{aesthetics}{Aesthetic this scale applies to. Defaults to \code{"tintshade"}.}
}
\value{
A ggplot2 scale object.
}
\description{
Map a variable to a lightness tint. The mapped value runs from \code{0} (black)
through \code{0.5} (the base hue) to \code{1} (white); the geom rescales these within
each hue group so the ramp restarts per group.
}
\examples{
library(ggplot2)
df <- data.frame(x = 1:3, y = 1:3, g = "a", item = c("lo", "mid", "hi"))
ggplot(df, aes(x, y, colour = g, tintshade = item)) +
  geom_point_tintshade(size = 4) +
  scale_tintshade_discrete(range = c(0.1, 0.9))
}
\seealso{
\code{\link[=guide_tintshade]{guide_tintshade()}}
}
